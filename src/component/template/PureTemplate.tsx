import React from 'react'
import { makeStyles } from '@material-ui/core/styles'

interface Props {
  id: string
}

const useStyles = makeStyles({})

export const PureTemplate = ({ id }: Props) => {
  return <></>
}

export default PureTemplate
